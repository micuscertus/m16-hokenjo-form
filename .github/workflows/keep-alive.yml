name: Keep Render service awake

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the form
        run: 'curl -sf -o /dev/null -w "status: %{http_code}\n" https://m16-hokenjo-form.onrender.com/'
